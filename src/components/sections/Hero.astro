---
/**
 * Hero con switch del target. Le due varianti di copy convivono nel markup
 * (data-only) e si alternano via CSS in base a html[data-target]: nessun
 * re-render al cambio. Le forme Bauhaus sono decorative (aria-hidden) e
 * animate da GSAP solo senza prefers-reduced-motion.
 */
import Button from "../ui/Button.astro";
import TargetSwitch from "../ui/TargetSwitch";
import DeskScene from "./DeskScene.astro";
---

<section class="hero" aria-labelledby="hero-titolo">
  <div class="hero__forme" aria-hidden="true">
    <span class="forma forma--cerchio" data-parallax="24"></span>
    <span class="forma forma--semicerchio" data-spin data-parallax="40"></span>
    <span class="forma forma--quarto" data-spin data-parallax="32"></span>
    <span class="forma forma--barra" data-spin data-angle="-45" data-parallax="20"></span>
  </div>

  <div class="container-site hero__grid">
    <div>
      <p class="eyebrow text-ink-2" data-hero-reveal>
        Web Developer · AI Web Designer
      </p>
      <h1 id="hero-titolo" class="mt-4">
        <span class="block">Ciao, sono Luigi.</span>
        <span class="hero__riga hero__riga--intro block" data-only="business"
          >Aiuto le aziende a vendere online.</span
        >
        <span class="hero__riga hero__riga--dev block" data-only="dev"
          >Programmatore web junior. Lavoro ogni giorno su negozi online.</span
        >
        <span class="hero__riga hero__riga--business block" data-only="business"
          >Partiamo dai numeri del tuo negozio e decidiamo insieme cosa cambiare.</span
        >
      </h1>
      <div class="mt-8" data-hero-reveal>
        {/* client:idle: le visuali dello switch sono CSS-driven (html[data-target]),
            l'idratazione serve solo ai click: togliere React dalla finestra FCP→LCP
            vale ~5 punti Lighthouse mobile */}
        <TargetSwitch client:idle />
      </div>
      <div class="hero__cta" data-hero-reveal>
        <Button variant="primary" size="lg" href="#progetti">
          <span data-only="dev">Vedi il codice</span>
          <span data-only="business">Vedi i risultati</span>
        </Button>
        <Button variant="secondary" size="lg" href="/contatti">Contattami</Button>
      </div>

      {/* Un numero vero sopra la piega: i dati del caso reale stavano a due
          click da qui. Testo statico e non StatTile: senza conteggio non c'è
          animazione da far ripartire quando lo switch scopre questa riga. */}
      <p class="hero__prova" data-only="business" data-hero-reveal>
        <strong>+86% di fatturato</strong> da giugno a luglio, nel <a
          href="/progetti/caso-reale">caso reale</a
        >.
      </p>
    </div>

    <div class="hero__media">
      <DeskScene />
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    overflow: hidden;
    padding-block: 3rem 5rem;
  }

  @media (min-width: 1024px) {
    .hero {
      padding-block: 5rem 6.5rem;
    }
  }

  .hero__grid {
    position: relative;
    z-index: 1;
    display: grid;
    gap: 3.5rem;
  }

  @media (min-width: 1024px) {
    .hero__grid {
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: center;
      gap: 4rem;
    }
  }

  /* text-wrap: balance (globale sugli h1) interferisce con SplitText */
  #hero-titolo {
    text-wrap: wrap;
  }

  /* La riga che cambia col target: più piccola della prima, ink-2 in
     entrambi i percorsi (docs/design-tokens.md, palette a tre colori). */
  .hero__riga {
    margin-top: 0.75rem;
    color: var(--color-ink-2);
    font-size: clamp(1.625rem, 3.5vw, 2.75rem);
    font-weight: 500;
  }

  /* La riga aggiuntiva del percorso business, sopra quella colorata: stessa
     misura e stesso colore, il testo non compete con la riga sotto. */
  .hero__riga--intro {
    color: var(--color-ink-2);
  }

  /* Le due CTA restano affiancate da 390 in su: misurate, 168 + 150 + 16 di
     gap = 334px, che stanno nei 350px utili a 390 ma non nei 320 di uno
     schermo da 360. Sotto quella soglia vanno in colonna, a larghezza piena
     e col primary sopra. flex-wrap resta la rete di sicurezza se un giorno
     un'etichetta si allunga. */
  .hero__cta {
    display: grid;
    gap: 0.75rem;
    margin-top: 1.5rem;
  }

  @media (min-width: 380px) {
    .hero__cta {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
    }
  }

  /* La prova sotto la CTA: secondaria rispetto al titolo, ma il numero in ink
     perché è la prima cosa verificabile che si incontra sulla home. */
  .hero__prova {
    margin-top: 1.25rem;
    max-width: 46ch;
    color: var(--color-ink-2);
    font-size: var(--text-body);
  }

  .hero__prova strong {
    color: var(--color-ink);
    font-weight: 600;
  }

  .hero__media {
    width: 100%;
  }

  @media (min-width: 1024px) {
    .hero__media {
      /* min 26rem: sotto, le etichette mono (px fissi) non stanno più
         nei varchi percentuali della scena */
      width: clamp(26rem, 38vw, 33rem);
      justify-self: end;
    }
  }

  /* --- Forme Bauhaus decorative: solo ink, più una sola in arancio --- */
  .hero__forme {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
  }

  .forma {
    position: absolute;
    will-change: transform;
  }

  .forma--cerchio {
    top: -28px;
    right: 6%;
    width: 84px;
    height: 84px;
    border-radius: 50%;
    background: var(--color-ink);
  }

  .forma--semicerchio {
    bottom: 32px;
    left: -46px;
    width: 140px;
    height: 70px;
    border-radius: 140px 140px 0 0;
    background: var(--color-arancio);
  }

  .forma--quarto {
    right: 18%;
    bottom: -44px;
    width: 130px;
    height: 130px;
    border-radius: 100% 0 0 0;
    background: var(--color-ink);
  }

  .forma--barra {
    right: -64px;
    bottom: 26%;
    width: 180px;
    height: 24px;
    border-radius: var(--radius-pill);
    background: var(--color-ink);
  }

  @media (min-width: 1024px) {
    .forma--cerchio {
      top: -40px;
      right: 34%;
      width: 120px;
      height: 120px;
    }

    .forma--semicerchio {
      bottom: 60px;
      left: -56px;
      width: 170px;
      height: 85px;
    }

    .forma--quarto {
      right: 8%;
      bottom: -56px;
      width: 170px;
      height: 170px;
    }

    .forma--barra {
      right: -70px;
      bottom: auto;
      top: 16%;
      width: 220px;
      height: 28px;
    }
  }
</style>

<style is:global>
  /* Switch target: stili globali perché il DOM è renderizzato dall'isola React.
     Lo stato attivo è guidato da html[data-target], non dallo stato React:
     così è giusto anche prima dell'idratazione. */
  .tswitch {
    position: relative;
    isolation: isolate;
    display: grid;
    grid-template-columns: 1fr 1fr;
    max-width: 26rem;
    padding: 4px;
    border: 2px solid var(--color-ink);
    border-radius: var(--radius-pill);
    background: var(--color-cream-2);
  }

  /* Il "thumb" che scorre da un'opzione all'altra */
  .tswitch::before {
    content: "";
    position: absolute;
    z-index: -1;
    top: 4px;
    left: 4px;
    width: calc(50% - 4px);
    height: calc(100% - 8px);
    border-radius: var(--radius-pill);
    background: var(--color-ink);
    transition: transform 0.25s var(--ease-brand);
  }

  html[data-target="business"] .tswitch::before {
    transform: translateX(100%);
  }

  .tswitch__opt {
    min-height: 40px;
    padding: 0.5rem 0.5rem;
    border: 0;
    border-radius: var(--radius-pill);
    background: none;
    color: var(--color-ink);
    font-family: var(--font-sans);
    font-size: 0.8125rem;
    font-weight: 600;
    line-height: 1.2;
    white-space: nowrap;
    cursor: pointer;
    transition: color 0.25s var(--ease-brand);
  }

  @media (min-width: 768px) {
    .tswitch__opt {
      font-size: 0.9375rem;
      padding-inline: 1rem;
    }
  }

  html:not([data-target="business"]) .tswitch__opt[data-value="dev"],
  html[data-target="business"] .tswitch__opt[data-value="business"] {
    color: var(--color-cream);
  }

  .tswitch__opt:focus-visible {
    outline: 2px solid var(--color-ink);
    outline-offset: 3px;
  }
</style>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";
  import { SplitText } from "gsap/SplitText";

  gsap.registerPlugin(ScrollTrigger, SplitText);

  // Con le View Transitions il modulo gira una volta sola, mentre la pagina si
  // monta e si smonta più volte: senza agganciarsi a astro:page-load, tornando
  // in home l'hero resterebbe senza animazioni, e i ScrollTrigger della visita
  // precedente resterebbero appesi a nodi che non esistono più.
  let mm: gsap.MatchMedia | undefined;

  function spegni() {
    mm?.revert();
    mm = undefined;
  }

  function avvia() {
    spegni();

    const hero = document.querySelector<HTMLElement>(".hero");
    if (!hero) return;

    mm = gsap.matchMedia();

    // Tutte le animazioni vivono qui: con prefers-reduced-motion resta tutto statico
    mm.add("(prefers-reduced-motion: no-preference)", () => {
      // Entrata: h1 per parole (SplitText, stagger 60ms), poi eyebrow/switch/CTA
      // e gli oggetti della scena (stagger 80ms). Budget totale < 1,2s.
      // Si parte a font caricati, così SplitText misura il testo giusto.
      let attiva = true;
      let split: SplitText | undefined;

      document.fonts.ready.then(() => {
        if (!attiva) return;

        const visibili = (sel: string) =>
          gsap.utils
            .toArray<HTMLElement>(hero.querySelectorAll(sel))
            .filter((el) => getComputedStyle(el).display !== "none");

        split = SplitText.create(visibili("#hero-titolo > span"), {
          type: "words",
          aria: "auto",
        });

        gsap
          .timeline({ defaults: { ease: "power3.out", duration: 0.5 } })
          .from(split.words, { autoAlpha: 0, y: 24, stagger: 0.06 }, 0)
          .from(
            visibili("[data-hero-reveal]"),
            { autoAlpha: 0, y: 24, stagger: 0.08, clearProps: "all" },
            0.12,
          )
          .from(
            visibili(".hotspot__body, .tile"),
            { autoAlpha: 0, y: 24, stagger: 0.08, clearProps: "all" },
            0.3,
          )
          .call(() => {
            split?.revert();
            split = undefined;
          });
      });

      gsap.utils.toArray<HTMLElement>(".forma").forEach((forma) => {
        const angolo = Number(forma.dataset.angle ?? 0);
        gsap.set(forma, { rotation: angolo });

        // Rotazione lenta continua: 60s per giro
        if (forma.dataset.spin !== undefined) {
          gsap.to(forma, {
            rotation: angolo + 360,
            duration: 60,
            ease: "none",
            repeat: -1,
          });
        }

        // Parallax leggero mentre l'hero scorre via (max 40px)
        gsap.to(forma, {
          y: () => -Number(forma.dataset.parallax ?? 24),
          ease: "none",
          scrollTrigger: {
            trigger: hero,
            start: "top top",
            end: "bottom top",
            scrub: true,
          },
        });
      });

      return () => {
        attiva = false;
        split?.revert();
        split = undefined;
      };
    });
  }

  document.addEventListener("astro:page-load", avvia);
  document.addEventListener("astro:before-swap", spegni);
</script>
